    <h2>Catégorie : <?= $categorie->getNom() ?></h2>
<table class="table table-striped table-vertical-align-middle">
    <thead>
        <tr>
            <th></th>
            <th></th>
            <th></th>
            <th></th> 
            <th></th> 
        </tr>
    </thead>

    <tbody>
        <td><img src="<?= $categorie->getPhoto() ?>" alt="<?= $categorie->getNom() ?>"/></td>
        <td><?= $categorie->getNom() ?></td>
        <td><?= $categorie->getDescription() ?></td>
        <td>
            <a class="bouton button btn btn-success" href="?page=admin_categorie_modif&id=<?= $categorie->getId() ?>">Modifier</a></td>
        <td>
            <form method="POST" class="register-form">
                <input type="hidden" name="id" value="<?= htmlentities( $categorie->getId() ) ?>">
                <button type="submit" class="bouton button btn btn-success" name="action" value="supp">Supprimer</button>
            </form>
        </td>
    </tbody>
</table>

<?php require('apps/admin_souscategories.php'); ?>